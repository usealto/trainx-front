import { TeamColorPipe } from './team-color.pipe';

describe('TeamColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamColorPipe();
    expect(pipe).toBeTruthy();
  });
});
