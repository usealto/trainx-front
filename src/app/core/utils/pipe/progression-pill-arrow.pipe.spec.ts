import { ProgressionPillArrowPipe } from './progression-pill-arrow.pipe';

describe('ProgressionPillArrowPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgressionPillArrowPipe();
    expect(pipe).toBeTruthy();
  });
});
